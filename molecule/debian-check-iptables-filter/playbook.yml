---
- name: converge
  hosts: instance
  tasks:
    - name: install Docker
      import_role:
        name: docker

    - name: flush iptables filter table
      iptables:
        table: filter
        flush: yes

    - name: fix iptables rules
      import_role:
        name: docker
      vars:
        docker_modules_dependencies_install: no
