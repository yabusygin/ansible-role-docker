---
- name: converge
  hosts: instance
  tasks:
    - name: install Docker
      import_role:
        name: docker

    - name: flush iptables nat table
      iptables:
        table: nat
        flush: yes

    - name: fix iptables rules
      import_role:
        name: docker
      vars:
        docker_modules_dependencies_install: no
